import "reflect-metadata";
import * as angular from 'angular';
export declare class ExampleThree implements angular.IController {
    static $inject: Array<string>;
    static Name: string;
    echo: string;
    imgClass: string;
    constructor();
    $onInit(): void;
}
